# StairGenerator (DS4)

> Independent Blender addon for generating stairs between two selected vertical faces.
>
> **Author:** This addon was written by the **DeepSeek V4 Flash** large language model.

## Features

- **Select 2 vertical faces** and generate parameterized stairs automatically
- **4 stair modes**:
  - `STRAIGHT` — single straight flight
  - `CURVED` — arc/winder flight
  - `L` — two flights with a landing and a user-set absolute turn angle
  - `U` — switchback (two parallel flights)
- **Full parameterization**:
  - Step count, width, depth (tread), height (riser)
  - Railing on/off, height, thickness, fill type (none / balusters / wall)
  - Railing style `SLOPE` (one straight handrail) or `STEPPED` (a flex point per step)
  - Classical **turned baluster** option (bulged lathe profile)
  - Landing platform options incl. L-turn angle and U slab turn
  - **Generate as Object** — build the stairs+railing into a separate `Stairs` object
    instead of fusing them into the current mesh

## Usage

1. Enter Edit Mode in Blender
2. Select exactly 2 faces that are perpendicular to the ground (vertical faces)
3. Select **StairGenerator (DS4)** tab in the Toolshelf
4. Adjust parameters as desired
5. Click **Add Stairs**

## Installation

1. Open Blender → Edit → Preferences → Add-ons
2. Click **Install** → select the `stair_builder.zip`
3. Enable the addon

## Architecture

```
stair_builder/
├── __init__.py              # Main addon entry point, UI panel, register/unregister
├── btools/
│   ├── __init__.py
│   ├── utils/
│   │   ├── __init__.py
│   │   ├── util_constants.py    # VEC_UP, VEC_RIGHT, VEC_DOWN constants
│   │   ├── util_common.py       # Common utilities (popup, crash_safe, etc.)
│   │   ├── util_mesh.py         # Mesh operations (sort, extrude, create_face)
│   │   ├── util_geometry.py     # Geometry computations (angles, distances)
│   │   ├── util_material.py     # Material linking utilities
│   │   ├── util_object.py       # Object creation utilities
│   │   ├── util_event.py        # Event handler utilities
│   │   └── devtools.py          # Debug tools
│   └── stair/
│       ├── __init__.py          # Module init, class registration
│       ├── stair_ops.py         # Main operator (BTOOLS_OT_add_stairs)
│       ├── stair_props.py       # Property groups (StairsProperty, RailProperty)
│       ├── stair_types.py       # Core geometry generation algorithms
│       ├── stair_railing.py     # Railing generation helpers
│       └── sizeoffset.py        # Size/offset property (placeholder)
└── README.md
```

## Credits

This addon was generated by the **DeepSeek V4 Flash** large language model.

## License

MIT

---

# 中文版說明 (Chinese)

# StairGenerator (DS4)

> 獨立 Blender 外掛：在兩張選取的垂直面之間生成樓梯。
>
> **作者：** 本外掛由 **DeepSeek V4 Flash** 大型語言模型撰寫。

## 功能特色

- **選取 2 張垂直面**並自動生成參數化樓梯
- **4 種樓梯型態**：
  - `STRAIGHT` — 單一直跑梯段
  - `CURVED` — 弧形/旋轉梯段
  - `L` — 兩段梯段 + 平台，可設定絕對轉角角度
  - `U` — 折返式（兩段平行梯段）
- **完整參數化**：
  - 台階數、寬度、深度（踏面）、高度（梯級）
  - 欄杆開關、高度、厚度、填滿類型（無 / 欄杆柱 / 牆面）
  - 欄杆樣式 `SLOPE`（一段直扶手）或 `STEPPED`（每階一個轉折點）
  - 古典**車枳欄杆柱**選項（中間鼓起的車削曲面）
  - 平台選項，包含 L 型轉角角度與 U 型平台轉向
  - **Generate as Object** — 將樓梯+欄杆建成獨立的 `Stairs` 物件，而非併入目前的網格

## 使用方式

1. 進入 Blender 的編輯模式 (Edit Mode)
2. 選取剛好 2 張垂直於地面的面（垂直面）
3. 在工具側欄選擇 **StairGenerator (DS4)** 分頁
4. 依需求調整參數
5. 點擊 **Add Stairs**

## 安裝方式

1. 開啟 Blender → Edit → Preferences → Add-ons
2. 點擊 **Install** → 選擇 `StairGenerator_(DS4).zip`
3. 啟用外掛

## 架構

```
stair_builder/
├── __init__.py              # 外掛主入口、UI 面板、register/unregister
├── btools/
│   ├── __init__.py
│   ├── utils/
│   │   ├── __init__.py
│   │   ├── util_constants.py    # VEC_UP、VEC_RIGHT、VEC_DOWN 常數
│   │   ├── util_common.py       # 通用工具（popup、crash_safe 等）
│   │   ├── util_mesh.py         # 網格操作（sort、extrude、create_face）
│   │   ├── util_geometry.py     # 幾何計算（角度、距離）
│   │   ├── util_material.py     # 材質連結工具
│   │   ├── util_object.py       # 物件建立工具
│   │   ├── util_event.py        # 事件處理工具
│   │   └── devtools.py          # 除錯工具
│   └── stair/
│       ├── __init__.py          # 模組初始化、類別註冊
│       ├── stair_ops.py         # 主要操作員（BTOOLS_OT_add_stairs）
│       ├── stair_props.py       # 屬性群組（StairsProperty、RailProperty）
│       ├── stair_types.py       # 核心幾何生成演算法
│       ├── stair_railing.py     # 欄杆生成輔助
│       └── sizeoffset.py        # 尺寸/偏移屬性（佔位）
└── README.md
```

## 版權聲明

本外掛由 **DeepSeek V4 Flash** 大型語言模型生成。

## 授權

MIT
